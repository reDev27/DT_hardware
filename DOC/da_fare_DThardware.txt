Java:
{
Stored procedure:
	{
		register user (aggiornare),							//fatto
		insert indirizzo, prodotto, ordine, 				//fatto
		override register,									//fatte
		acquisto,											//fatto
		prelievo prodotti(select),
		(...)
	};
	Java bean/DAO per tutte le procedure elencate sopra;	//fatto
	gestione prodotti, carrello e acquisti;					//fatto
	da modificare la gestione delle immagini lato server;	//fatto
	servlets;
}

Javascript:
{
	gestione visualizzazione contenuti;						//fatto
	gestione accesso e registrazione;
	gestione carrello;										//fatto
	algoritmo di ricerca;
	gestione acquisti;
	pagina visualizza i MIEI ordini(cliente);
	pagina visualizza gli ordini(admin);
	pagina registrazione;
	pagina personale utente;
	pagina informazioni generali;
	front-end.
}

Rifiniture
{
	aggiornare quantità categorie dinamicamente(aggiunta/eliminazione prodotto);
	aggiornare il numero di prodotti al momento dell'ordine;
	
}