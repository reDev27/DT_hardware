Java:
{
Stored procedure:
	{
		register user (aggiornare),							//fatto
		insert indirizzo, prodotto, ordine, 				//fatto
		override register,									//fatte
		acquisto,											//fatto
		prelievo prodotti(select),							//fatto
		(...)
	};
	Java bean/DAO per tutte le procedure elencate sopra;	//fatto
	gestione prodotti, carrello e acquisti;					//fatto
	da modificare la gestione delle immagini lato server;	//fatto
	servlets;
}

Javascript:
{
	gestione visualizzazione contenuti;						//fatto
	gestione accesso e registrazione;						//fatto
	gestione carrello;										//fatto
	algoritmo di ricerca;									//fatto
	gestione acquisti;										//fatto
	pagina visualizza i MIEI ordini(cliente);				//fatto
	pagina visualizza gli ordini(admin);					//fatto
	pagina registrazione;									//fatto
	pagina personale utente;								//fatto
	pagina informazioni generali;							//fatto
	front-end.												//fatto
}

Rifiniture
{
	aggiornare quantità categorie dinamicamente(aggiunta/eliminazione prodotto);				//fatto
	fare in modo che se si acquista più della quantità consentita si visualizzi un errore; 		//fatto
	simbolo euro in homepage nei prodotti;														//fatto
	icona disponibile/esaurito pagina prodotti;													//fatto
	arrotondare cifre in tutte le pagine;														//fatto
	qualche pagina di errore(opzionale);														
}













